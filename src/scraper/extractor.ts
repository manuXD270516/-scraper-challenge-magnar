/**
 * scraper/extractor.ts — Función pura HTML → Documento[] (cheerio), parsing defensivo,
 * validación contra schema. Testeable con fixtures. Spec: specs/F3_extraccion.md. STUB (Fase 1).
 */
export {};
