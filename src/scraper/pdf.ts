/**
 * scraper/pdf.ts — Resolución del request de descarga (ServletDescarga?uuid=<UUID>),
 * stream a disco, validación magic bytes %PDF, naming descriptivo + dedupe, skip idempotente.
 * Nace envuelto en la política de retry (F5). Spec: specs/F4_descarga_pdf.md. STUB (Fase 1).
 */
export {};
