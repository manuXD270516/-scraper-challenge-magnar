/**
 * state/store.ts — state.json (checkpoint atómico: write tmp + rename) + failed.json
 * (ledger append-only). Re-ejecutar jamás repite trabajo.
 * Ancla: doc 01 §D-3. Spec: specs/F6_estado_reanudacion.md. STUB (Fase 1).
 */
export {};
